## An example to test the use of react testing library

### Setting up

1. Run `npm install`
2. Run `npm run test` to run test suites
3. Run `npm run server` to run the server api
4. Run `npm run start` to run a react app